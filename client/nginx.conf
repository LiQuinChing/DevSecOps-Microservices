server {
    listen 8080;
    server_name localhost;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This is the magic line that fixes the 404 for React Router!
        try_files $uri $uri/ /index.html;
    }
}